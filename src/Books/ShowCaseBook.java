package Books;
import java.util.Date;

public class ShowCaseBook extends Book{
    public ShowCaseBook(String isbn, String title, double price, Date publishDate) {
        super(isbn, title, price, publishDate );
    }
}
